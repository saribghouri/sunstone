export enum ButtonType {
  primary = "primary",
  dashed = "dashed",
  link = "link",
  text = "text",
  default = "default",
}

export enum ButtonSize {
  small = "small",
  middle = "middle",
  large = "large",
}

export enum ButtonHTMLType {
  button = "button",
  submit = "submit",
  reset = "reset",
}
